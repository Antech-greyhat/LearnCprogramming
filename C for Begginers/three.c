#include <stdio.h>

int main() {

    int a, b, c, l;

    printf("input three numbers");

    scanf("%d %d %d", &a, &b, &c);

    l = (l>c) ? l:c;

    printf("largest number is : %d" , l);

    return 0;
}