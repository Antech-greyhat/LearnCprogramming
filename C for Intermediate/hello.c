//preprocessor directives are files needed to perform tasks in the function
#include <stdio.h>

int main () {
    printf("Hello, World");

//data types are used to declare variables and functions in C. They specify the type of data that can be stored in a variable or returned by a function. Common data types include int, float, char, and double.

int age =20; //declaring an integer variable named age and assigning it the value of 20

char grade = 'A'; //declaring a character variable named grade and assigning it the value of 'A'

int mobileNumber = 0714452396; //declaring an integer variable named mobileNumber and assigning it the value of 0714452396

char _language = 'C'; //declaring a character variable named _language and assigning it the value of 'C'

int my_age = 18; //declaring an integer variable named my_age and assigning it the value of 18
    return 0;

    // taking inputs from the user

    int userAge;

    scanf("%d", &userAge); //taking input from the user and storing it in the variable userAge
}
